# requirements.txt

# Core web framework
flask

# For interacting with OpenAI-compatible APIs (like Typhoon)
openai

# For converting PDF pages to images
pdf2image

# For data manipulation and creating Excel files
pandas
openpyxl

# For image processing and enhancement
opencv-python-headless
numpy
Pillow

# For loading environment variables from .env file
python-dotenv

# Production-ready web server (optional for local dev, good for deployment)
gunicorn

# For PDF text/table extraction (if you add other features later)
pdfplumber

# For handling Cross-Origin Resource Sharing (CORS) in Flask
flask-cors